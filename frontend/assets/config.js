/* ============================================================
   APlusZ — Affiliate Config (Step 12)
   File: frontend/assets/config.js
   Save: D:\Destop\AplusZ\frontend\assets\config.js

   Fill in real IDs once accounts are approved.
   Placeholders work for testing — no revenue until replaced.
   ============================================================ */

(function () {
  window.APlusZ = window.APlusZ || {};
  window.APlusZ.config = {
    affiliates: {
      kiwi:       'placeholder',  // https://www.kiwi.com/affiliates  (instant approval)
      skyscanner: 'placeholder',  // https://www.travelpayouts.com/  (Skyscanner via TP)
      kayak:      'aplusz',       // https://www.kayak.com/affiliate (or via Travelpayouts)
      booking:    'placeholder'   // https://www.booking.com/affiliate-program (or via TP)
    }
  };
})();
